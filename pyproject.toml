[project]
name = "issue-discussion-platform"
version = "0.1.0"
description = "Issue Discussion Platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.12.0",
]
