# Public Study API origin for participant routes (local: http://127.0.0.1:8000)
NEXT_PUBLIC_STUDY_API_ORIGIN=http://127.0.0.1:8000

# Staff login (Supabase Auth; publishable key only — never the service role key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Sample invitation token for local end-to-end smoke (matches backend in-memory seed)
# Open /invite/demo-campus-speech-001-invitation-token-for-contract-tests after starting the API.
SAMPLE_INVITATION_TOKEN=demo-campus-speech-001-invitation-token-for-contract-tests
